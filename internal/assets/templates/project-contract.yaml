# Generated by ezHarness install.sh. Copy into project ownership by editing .harness/project-contract.yaml.
service: {{PROJECT_NAME_JSON}}
owners:
  - "TODO: owner or team"
build:
  verify: {{VERIFY_COMMAND_JSON}}
env_rules:
  - id: no-network-during-verify
    applies_to: verify
    reminder: "Verification must not require network access; use checked-in or preinstalled dependencies."
    severity: blocking
risk_level: med
# risk_paths: repo-relative globs that REQUIRE worker+verifier evidence to
# commit/push. A change touching none of them is allowed with a warning.
# Omit this block to gate every change (strict). Example:
# risk_paths:
#   - "src/**"
#   - "migrations/**"
